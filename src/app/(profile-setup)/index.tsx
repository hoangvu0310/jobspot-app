import { DetailScreenLayout } from '@/components/layout'
import { View } from 'react-native'

export default function SetupProfileWelcomeScreen() {
	return (
		<DetailScreenLayout titleKey={'profileSetup.setupYourProfile'}>
			<View></View>
		</DetailScreenLayout>
	)
}
